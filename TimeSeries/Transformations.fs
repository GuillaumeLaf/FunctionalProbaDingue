namespace Timeseries

open FSharpPlus
open FSharpPlus.Data
open TimeseriesType
open TimeseriesState

module Transformations = 
    let x = 0
    //let fractionalDifference alphas = Array.map2 (-) <!> currentElements <*> lagElements 1        : State<(int * TS),float32>

